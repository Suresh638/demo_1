console.log(Math.PI.toFixed(2));
// trunc method removes all the values after the  decimal point.
console.log(Math.trunc(2.5));

// round method
console.log(Math.fround(2.4));

// floor method
console.log(Math.floor(2.8));

// ceil method
console.log(Math.ceil(2.54));

// pow first argument base and the  second argument is the power value
console.log(Math.pow(3,2));

// gives the minimum value in the group of values.
console.log(Math.min(0,4,58,4,-1));

// maximum value of a given values in a list
console.log(Math.max(5454,52,1,2,4));

// random value 0 to one (where one is exclusive)
console.log(Math.ceil(Math.random()*100));


